# `hooks/` — global reusable hooks

Starts empty on purpose. **Promote on the second reuse, not the first.**

Cross-feature hooks only. A hook used by exactly one screen belongs in
`containers/<feature>/hooks/` — see `containers/dashboard/hooks/useDashboard.ts`
for the shape to copy.
